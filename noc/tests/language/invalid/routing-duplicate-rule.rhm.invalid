#lang rhombus
// Intentionally duplicates a routing rule name to exercise expansion diagnostics.
// SPDX-License-Identifier: Apache-2.0

import:
  "../../../language/main.rhm" open

def bad = routing:
  rule repeated:
    origin injection
  rule repeated:
    origin forwarding
