#lang rhodium
// Rejects an opcode from the wrong hardware-enum namespace in a capability list.
// SPDX-License-Identifier: Apache-2.0

import:
  lib("chi/main.rhdl") open

def capabilities = CHIChannelCapabilities(~req: [CHIRspOpcode.Comp])
