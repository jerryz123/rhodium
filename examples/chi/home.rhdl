#lang rhodium
// Demonstrates a multi-subordinate, multi-beat initial-profile HN-I bridge.
// SPDX-License-Identifier: Apache-2.0

import:
  lib("chi/main.rhdl") open

def home_flit = CHIFlitParams()
def requester_endpoint = CHIICNPortParams(
  "home-requester",
  3,
  CHINodeKind.RNI,
  ~emits: CHIChannelCapabilities(
    ~rsp: [CHIRspOpcode.DBIDResp, CHIRspOpcode.Comp],
    ~dat: [CHIDatOpcode.CompData]
  ),
  ~supports: CHIChannelCapabilities(
    ~req: [CHIReqOpcode.ReadNoSnp,
           CHIReqOpcode.WriteNoSnpFull,
           CHIReqOpcode.WriteNoSnpPtl],
    ~dat: [CHIDatOpcode.NonCopyBackWriteData]
  ),
  ~max_outstanding: 2
)
def subordinate_endpoint = CHIICNPortParams(
  "home-subordinate",
  9,
  CHINodeKind.SNI,
  ~emits: CHIChannelCapabilities(
    ~req: [CHIReqOpcode.ReadNoSnp,
           CHIReqOpcode.WriteNoSnpFull,
           CHIReqOpcode.WriteNoSnpPtl],
    ~dat: [CHIDatOpcode.NonCopyBackWriteData]
  ),
  ~supports: CHIChannelCapabilities(
    ~rsp: [CHIRspOpcode.DBIDResp, CHIRspOpcode.Comp],
    ~dat: [CHIDatOpcode.CompData]
  ),
  ~max_outstanding: 2
)
def second_subordinate_endpoint = CHIICNPortParams(
  "home-subordinate-1",
  10,
  CHINodeKind.SNI,
  ~emits: CHIChannelCapabilities(
    ~req: [CHIReqOpcode.ReadNoSnp,
           CHIReqOpcode.WriteNoSnpFull,
           CHIReqOpcode.WriteNoSnpPtl],
    ~dat: [CHIDatOpcode.NonCopyBackWriteData]
  ),
  ~supports: CHIChannelCapabilities(
    ~rsp: [CHIRspOpcode.DBIDResp, CHIRspOpcode.Comp],
    ~dat: [CHIDatOpcode.CompData]
  ),
  ~max_outstanding: 2
)
def home = CHIHomeParams(
  "test-hni",
  5,
  CHINodeKind.HNI,
  ~max_outstanding: 2
)
def subordinate_service = CHISubordinateServiceParams(
  subordinate_endpoint,
  [AddressSet(0x80000000, 0x0fffffff)],
  [CHIRequestSupport(CHIReqOpcode.ReadNoSnp,
                     TransferSizes(1, 64)),
   CHIRequestSupport(CHIReqOpcode.WriteNoSnpFull,
                     TransferSizes(1, 64)),
   CHIRequestSupport(CHIReqOpcode.WriteNoSnpPtl,
                     TransferSizes(1, 64))]
)
def second_subordinate_service = CHISubordinateServiceParams(
  second_subordinate_endpoint,
  [AddressSet(0x90000000, 0x0fffffff)],
  subordinate_service.requests
)
def subordinate_map = CHISubordinateMap(
  home_flit,
  [subordinate_service, second_subordinate_service]
)
def home_params = CHIHNIParams(home,
                               [requester_endpoint],
                               subordinate_map)
def home_design = elaborate(CHIHNI(home_params))

export:
  home_flit
  requester_endpoint
  subordinate_endpoint
  second_subordinate_endpoint
  home
  subordinate_service
  second_subordinate_service
  subordinate_map
  home_params
  home_design
