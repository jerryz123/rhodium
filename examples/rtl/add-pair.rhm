#lang rhombus
// Defines a concise user combinator by importing only the combinational frontend layer it needs.
// SPDX-License-Identifier: Apache-2.0

import:
  lib("rhodium/frontend/layers/comb.rhm") open

export:
  add_pair

fun add_pair(left :: HardwareEntity, right :: HardwareEntity):
  left + right
