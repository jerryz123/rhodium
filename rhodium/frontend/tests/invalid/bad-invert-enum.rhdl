#lang rhodium
// Rejects bang inversion for a nominal enum that has no bitwise capability.
// SPDX-License-Identifier: Apache-2.0

hardware_enum State:
  Idle
  Busy

circuit BadInvertEnum():
  input state: State
  output inverted: State
  inverted <== !state

def design = elaborate(BadInvertEnum())
