#lang rhodium
// Must reject an explicit compatibility provider that is not a declared interface role.
// SPDX-License-Identifier: Apache-2.0

interface BadProvider():
  role producer
  role consumer
  provider observer

  producer -> consumer:
    payload: Bits(8)

def invalid = BadProvider()
