#lang rhodium
// Rejects direct source references to the implicit sync-circuit reset.
// SPDX-License-Identifier: Apache-2.0

sync_circuit ManualResetReference():
  output value: Bits(1)
  def leaked_reset = reset
  value <== bits(0, 1)

def design = elaborate(ManualResetReference())
