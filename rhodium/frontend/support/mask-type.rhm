#lang rhombus
// Defines the shared structural protocol implemented by fixed-width lane-set masks.
// SPDX-License-Identifier: Apache-2.0

export:
  MaskType

// Layers can require mask semantics without importing the frontend layer that
// owns the public Mask type and its Bool-valued indexing surface.
interface MaskType:
  method mask_width()
