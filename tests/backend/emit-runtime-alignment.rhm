#lang rhombus
// Emits bounded runtime alignment checks across address and exponent widths.

import:
  "../../rhodium/backend/circt.rhm" open
  "../../rhodium/frontend/kernel.rhm" as embedded
  "../frontend/std-bits-fixture.rhdl" as fixture

print(emit_circt(embedded.run_elaboration(fun (): fixture.RuntimeAlignmentFixture())))
