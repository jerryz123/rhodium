#lang rhombus
// Emits the composed vector bank and SIMD fixtures at three VLEN specializations.

import:
  "../../rhodium/backend/circt.rhm" open
  "../../rhodium/frontend/kernel.rhm" as embedded
  "../../cores/rv5stage/tests/vector-fixture.rhdl" as fixture

def design = embedded.run_elaboration(fun (): fixture.RV5StageVectorFixture())
print(emit_circt(design))
