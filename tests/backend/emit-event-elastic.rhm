#lang rhombus
// Emits traced elastic lanes and unmodified reference lanes for differential simulation.

import:
  "../../rhodium/backend/circt.rhm".emit_circt
  "../../rhodium/event/main.rhm".instrument_events
  "../frontend/event-elastic-fixture.rhdl" as fixture

print(emit_circt(instrument_events(fixture.elastic).instrumented.design))
