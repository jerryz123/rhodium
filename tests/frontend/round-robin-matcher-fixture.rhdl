#lang rhodium
// Elaborates a three-input, two-output output-greedy round-robin matching fixture.

import:
  lib("flow/matcher.rhdl") open

def design = elaborate(OutputGreedyRoundRobinMatcher(3, 2))

export:
  design
