#lang rhombus
// Loads the use_static flow fixture, including reusable always-capture Valid pipe stages.

import:
  "std-flow-static.rhdl" as fixture

check fixture.FlowStaticInformation is_a Function ~is #true
