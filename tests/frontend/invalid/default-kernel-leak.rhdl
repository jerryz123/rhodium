#lang rhodium
// Ensures the curated standard profile does not implicitly expose raw elaboration-kernel entry points.
// SPDX-License-Identifier: Apache-2.0

def design = run_elaboration(fun (): #void)
