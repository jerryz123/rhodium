#lang rhodium
// Rejects a clocked effect in a sync circuit that uses a second clock input.
// SPDX-License-Identifier: Apache-2.0

sync_circuit ForeignClockedState():
  input other_clock: Clock

  reg state(Bits(1), ~clock: other_clock)
  state <== state

def design = elaborate(ForeignClockedState())
