#lang rhodium
// Must reject behavioral support attributed to a role other than the provider.
// SPDX-License-Identifier: Apache-2.0

interface BaseProtocol():
  role producer
  role consumer

  producer -> consumer:
    payload: Bits(8)

interface BadSupport() refines BaseProtocol():
  supports consumer: BaseProtocol()

def invalid = BadSupport()
