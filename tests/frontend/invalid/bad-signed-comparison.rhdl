#lang rhodium
// Rejects signed ordering across different explicit widths.
// SPDX-License-Identifier: Apache-2.0

circuit BadSignedComparison():
  input left: SInt(4)
  input right: SInt(8)
  output result: Bool
  result <== (left < right)

def design = elaborate(BadSignedComparison())
