#lang rhodium
// Rejects a combinational feedback loop that crosses an identity-child boundary.
// SPDX-License-Identifier: Apache-2.0

circuit Identity():
  input a: Bits(1)
  output y: Bits(1)
  y <== a

circuit Top():
  inst u(Identity())
  u.a <== u.y

def design = elaborate(Top())
