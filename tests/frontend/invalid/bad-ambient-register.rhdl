#lang rhodium
// Rejects ambient register syntax outside an active sync circuit.
// SPDX-License-Identifier: Apache-2.0

circuit BadAmbientRegister():
  output value: Bits(1)
  reg state(Bits(1))
  state <== state
  value <== state

def design = elaborate(BadAmbientRegister())
