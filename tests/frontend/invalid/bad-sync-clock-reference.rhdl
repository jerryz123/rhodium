#lang rhodium
// Rejects direct source references to the implicit sync-circuit clock.
// SPDX-License-Identifier: Apache-2.0

sync_circuit ManualClockReference():
  output value: Bits(1)
  def leaked_clock = clock
  value <== bits(0, 1)

def design = elaborate(ManualClockReference())
