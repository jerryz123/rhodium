#lang rhodium
// Invalidly relies on an ordinary nested circuit inheriting its parent's sync domain.
// SPDX-License-Identifier: Apache-2.0

sync_circuit Parent():
  circuit Child():
    reg state(~init: bits(0, 8))
    #void

  Child()

def design = elaborate(Parent())
