#lang rhodium
// Invalidly captures parent hardware directly inside a nested child module.
// SPDX-License-Identifier: Apache-2.0

circuit Parent():
  input source: Bits(8)

  circuit Child():
    output leaked: Bits(8)
    leaked <== source

  Child()

def design = elaborate(Parent())
