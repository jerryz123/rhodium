#lang rhodium
// Rejects a non-integer interface endpoint array length.
// SPDX-License-Identifier: Apache-2.0

import:
  lib("rhodium/std/ready-valid.rhdl") open

circuit BadInterfaceArrayCount():
  interface links["two"](Decoupled(Bits(8)), ~role: consumer)
  #void

elaborate(BadInterfaceArrayCount())
