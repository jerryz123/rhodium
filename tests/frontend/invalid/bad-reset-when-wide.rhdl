#lang rhodium
// Rejects a scoped reset condition wider than one bit.
// SPDX-License-Identifier: Apache-2.0

sync_circuit WideResetScope():
  input condition: Bits(2)
  output value: Bits(1)
  reset_when(condition):
    value <== bits(0, 1)

def design = elaborate(WideResetScope())
